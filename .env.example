# This file is an example of what your .env might look like.
# Since this MCP server doesn't require an API key (it uses yfinance),
# there are no required environment variables.
# 
# However, you could add custom configuration here in the future.
LOG_LEVEL=INFO
